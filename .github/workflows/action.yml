#File: .github/workflows/python.yml
name: Python package

on: [push,pull_request]

jobs:

  build-and-deploy:

    runs-on: ubuntu-latest

    steps:

    # checkout the repo

    - name: Cloning repository

      uses: actions/checkout@master

    - name: Get current run metadata using curL

      run: curl -D headers.txt -SL https://oryxsdksdev.blob.core.windows.net/python/python-3.8.0.tar.gz --output 

    - name: App Service Web App Build Action

      uses: Azure/appservice-build@zqian/test-new-image 
      
      
